/* eslint-disable no-magic-numbers */

export default () => ({
  fab: {
    height: 40,
    width: 40,
    float: 'right',
    // position: 'fixed',
    top: -30,
    // left: 'calc(50% - 30px)',
    marginLeft: 5,
  },
});
